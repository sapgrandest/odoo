**Subject: Request for API access and account credentials**

Hi,

As part of our integration with your platform, we would like to access your API to retrieve the product catalog (references, prices, availability).

Based on the Web Service documentation you provided (Dokumentacja WS MP.pdf), we understand that API authentication requires a Fiks (customer account number) and a Motonet number. However, we were unable to find these credentials in our account.

We also tried calling the Web Service endpoints directly (`https://195.242.186.3/MotoBiznesWS/`) but the requests are timing out — the TCP connection on port 443 is accepted, but the server does not process the HTTP requests. This may indicate that our IP address is not authorized to reach the service.

We also reviewed the available API endpoints on your developer portal but could not find any endpoint allowing us to retrieve the full product catalog. It is possible that this feature is not exposed through the API, or that it requires a specific access level that has not been granted to our account yet.

Could you provide us with:
- Our Fiks and Motonet account numbers required for API authentication
- Clarification on whether it is possible to retrieve the full product catalog via the API, and if so, how
- Confirmation of whether access to the Web Service endpoints requires an IP authorization, and if so, the process to get our IP whitelisted

Our account: SAP GRAND EST

Please don't hesitate to reach out if you need any further information.

Best regards,
